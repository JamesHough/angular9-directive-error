import { CleanUnusualCharactersDirective } from './clean-unusual-characters.directive';

describe('CleanUnusualCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new CleanUnusualCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
